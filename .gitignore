# Generated by `npm run seed` in backend/ — a local dev database seeded
# with demo data, not source. Committing it would mean every developer
# inherits whatever fleet state the last person happened to leave behind.
backend/*.db
backend/*.db-journal
backend/*.db-wal
backend/*.db-shm

node_modules/
.env
.env.local
.DS_Store
