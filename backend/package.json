{
  "name": "tfs-logistics-backend",
  "version": "1.0.0",
  "description": "Production-shaped REST API for TFS Logistics / Mercury - every touch point, WSW, and non-linear flow implemented, with auth, caching, and a full test suite. Runs on PostgreSQL: node-postgres against DATABASE_URL, or an embedded PostgreSQL (PGlite) when that is unset, so tests and a first local run need no server.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node src/seed.js",
    "test": "node --test \"src/__tests__/*.test.js\"",
    "test:watch": "node --test --watch \"src/__tests__/*.test.js\"",
    "test:pg": "node scripts/require-database-url.js && node --test --test-concurrency=1 src/__tests__/stateMachine.test.js src/__tests__/api.test.js"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
